# CE or EE
EDITION=CE

# Database connection string
# SQLite (default, good for local dev):
DATABASE_URL=sqlite:///./open_aicser.db
# PostgreSQL (production):
# DATABASE_URL=postgresql://user:password@localhost:5432/open_aicser

# JWT signing secret — use a long random string in production
# Generate one: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=

# JWT token lifetime in minutes (default: 60)
ACCESS_TOKEN_EXPIRE_MINUTES=60

# Allowed CORS origins, comma-separated (default: localhost:3000)
# CORS_ORIGINS=http://localhost:3000,https://yourdomain.com
